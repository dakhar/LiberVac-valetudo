
export const TimersHelp = `
## Timers

Timers allow you to execute a task at a specified time (UTC).<br/>
If the system time isn't plausible (e.g. like when it has never been synced), no timers will be executed.

Please note that timers are evaluated and stored as UTC. They are only displayed in your current browser timezone.<br/>
This means that they will not follow any DST rules and thus will have to be updated manually if so required.
`;

export const TimersHelpRu = `
## Таймеры

Таймеры позволяют выполнять задачу в заданное время (UTC).<br/>
Если системное время неправдоподобно (например, когда оно ни разу не синхронизировалось), ни один таймер не будет выполнен.

Обратите внимание, что таймеры вычисляются и хранятся в формате UTC. Они лишь отображаются в текущем часовом поясе вашего браузера.<br/>
Это означает, что они не учитывают правила перехода на летнее время и поэтому при необходимости их придётся обновлять вручную.
`;
